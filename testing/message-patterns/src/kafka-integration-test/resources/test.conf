bus {
    busType = "KAFKA"
    kafkaProperties {
        common {
            bootstrap.servers = "localhost:9092"
        }
    }
}

instance.id = 1
topic.prefix = ""

subscription {
    poll.timeout = 1000
    commit.retries = 4
}

publisher {
}

