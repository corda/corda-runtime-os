plugins {
    id 'corda.common-publishing'
    id 'corda.common-library'
}

description 'Injectable clock factory'

dependencies {
    compileOnly "org.osgi:org.osgi.service.component.annotations:$osgiServiceComponentVersion"
    compileOnly "org.osgi:org.osgi.service.component:$osgiServiceComponentVersion"
    implementation "net.corda.kotlin:kotlin-stdlib-jdk8-osgi:$kotlinVersion"

    implementation project(':libs:utilities')
}