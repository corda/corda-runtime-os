{
  "type": "record",
  "name": "PublicKeyRpcQuery",
  "namespace": "net.corda.data.crypto.wire.ops.rpc",
  "doc": "Request to find public matching the alias, if the key is not found it returns null.",
  "fields": [
    {
      "name": "alias",
      "type": "string"
    }
  ]
}