package net.corda.flow.statemachine.impl.transitions

import net.corda.flow.statemachine.FlowIORequest

// How much should this resemble the C4 code?
// If feel like the code will end up looking closer to the C4 code the more features we have to add
fun transition(request: FlowIORequest)