package net.corda.virtualnode.rpcops

import net.corda.lifecycle.Lifecycle

/** Manages the RPC operations for virtual node management via the HTTP RPC gateway. */
interface VirtualNodeRPCOpsService : Lifecycle