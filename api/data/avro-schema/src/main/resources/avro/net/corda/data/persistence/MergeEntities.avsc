{
  "type": "record",
  "name": "MergeEntities",
  "doc": "Merge (update) a set of entities with new values.  See also {@link EntityRequest#request}",
  "namespace": "net.corda.data.persistence",
  "fields": [
    {
      "name": "entities",
      "type": {
        "type" : "array",
        "items": "bytes"
      },
      "doc": "the serialized entity values, each on in AMQP format"
    }
  ]
}
