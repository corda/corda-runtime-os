package net.corda.crypto.persistence

import org.junit.jupiter.api.Test
import org.mockito.kotlin.mock

class TestCryptoRepositoryFactoryImpl {
    @Test
    fun `go go go`() {
        CryptoRepositoryFactoryImpl(mock(), mock(), mock(), mock())
    }
}