bus {
    busType = "KAFKA"
    kafkaProperties {
        common {
            bootstrap.servers = "localhost:9092"
        }
    }
}

instance.id = 1
topic.prefix = ""

subscription {
    pollTimeout = 1000
    commitRetries = 4
}

publisher {
    clientId = "clientId123"
    transactional = true
}

