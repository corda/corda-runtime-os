imagePullSecrets:
  - docker-registry-cred

kafka:
  bootstrapServers: prereqs-kafka:9092

db:
  cluster:
    host: prereqs-postgresql
    existingSecret:
      prereqs-postgresql

resources:
  requests:
    memory: 620Mi
    cpu: 500m
  limits:
    memory: 1250Mi
    cpu: 1000m