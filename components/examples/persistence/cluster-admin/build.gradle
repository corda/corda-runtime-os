plugins {
    id 'com.r3.internal.gradle.plugins.r3Publish'
    id 'corda.common-library'
}

dependencies {
    compileOnly 'org.osgi:osgi.annotation'
    compileOnly 'org.osgi:org.osgi.service.component.annotations'

    implementation platform("net.corda:corda-api:$cordaApiVersion")
    implementation 'net.corda.kotlin:kotlin-stdlib-jdk8-osgi'
    implementation "com.typesafe:config:$typeSafeConfigVersion"
    implementation 'net.corda:corda-base'
    implementation "net.corda:corda-avro-schema:$cordaApiVersion"

    implementation project(":libs:configuration:configuration-core")
    implementation project(":libs:db:db-admin")
    implementation project(":libs:db:db-admin-impl")
    implementation project(":libs:messaging:messaging")
    implementation project(":libs:lifecycle:lifecycle")

    // reference this to be able to pick up the liquibase files
    //  TODO: is this the right thing? Where should liquibase files come from in the "real" worker?
    implementation project(":components:examples:persistence:config-admin")
}

description 'Cluster Admin Component'
