bus {
    busType = "KAFKA"
    kafkaProperties {
        common {
            bootstrap.servers = "localhost:9092"
        }
    }
}

instanceId = 1
topicPrefix = ""

maxAllowedMessageSize = 10000000

subscription {
    pollTimeout = 1000
    commitRetries = 4
}

publisher {
    clientId = "clientId123"
    transactional = true
}

