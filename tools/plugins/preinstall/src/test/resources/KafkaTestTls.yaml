kafka:
  bootstrapServers: "localhost:9093"
  topicPrefix: ""
  tls:
    enabled: true
    truststore:
      password:
        valueFrom:
          secretKeyRef:
            name: "kafka-truststore-password"
            key: "kafka-truststore-password"
      type: "PKCS12"
  sasl:
    enabled: false