package net.corda.rest.server.impl.apigen.processing.openapi.schema.model

enum class DataFormat {
    INT32, INT64, FLOAT, DOUBLE, STRING, BYTE, BINARY, DATE, DATETIME, DURATION, PASSWORD, EMAIL, UUID
}