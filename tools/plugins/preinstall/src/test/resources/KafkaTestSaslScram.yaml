kafka:
  bootstrapServers: "localhost:9093"
  topicPrefix: ""
  tls:
    enabled: false
  sasl:
    mechanism: "SCRAM"
    enabled: true
    username:
      value: ""
    password:
      valueFrom:
        secretKeyRef:
          name: ""
          key: ""

workers:
  crypto:
    kafka:
      sasl:
        username:
          value: "alice"
        password:
          valueFrom:
            secretKeyRef:
              name: "kafka-sasl"
              key: "kafka-sasl-password"

  db:
    kafka:
      sasl:
        username:
          value: "alice"
        password:
          valueFrom:
            secretKeyRef:
              name: "kafka-sasl"
              key: "kafka-sasl-password"

  flow:
    kafka:
      sasl:
        username:
          value: "alice"
        password:
          valueFrom:
            secretKeyRef:
              name: "kafka-sasl"
              key: "kafka-sasl-password"

  membership:
    kafka:
      sasl:
        username:
          value: "alice"
        password:
          valueFrom:
            secretKeyRef:
              name: "kafka-sasl"
              key: "kafka-sasl-password"
  rest:
    kafka:
      sasl:
        username:
          value: "alice"
        password:
          valueFrom:
            secretKeyRef:
              name: "kafka-sasl"
              key: "kafka-sasl-password"

  p2pGateway:
    kafka:
      sasl:
        username:
          value: "alice"
        password:
          valueFrom:
            secretKeyRef:
              name: "kafka-sasl"
              key: "kafka-sasl-password"

  p2pLinkManager:
    kafka:
      sasl:
        username:
          value: "alice"
        password:
          valueFrom:
            secretKeyRef:
              name: "kafka-sasl"
              key: "kafka-sasl-password"