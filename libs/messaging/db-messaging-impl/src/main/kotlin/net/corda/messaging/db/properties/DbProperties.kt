package net.corda.messaging.db.properties

class DbProperties {
    companion object {
        const val JDBC_URL = "db.producer.jdbc.url"
        const val DB_USERNAME = "db.producer.username"
        const val DB_PASSWORD = "db.producer.password"
    }
}