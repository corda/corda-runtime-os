bootstrap:
  db:
    databases:
    - id: "default"
      username:
        value: "postgres"
      password:
        valueFrom:
          secretKeyRef:
            name: "postgres"
            key: "postgres-password"
config:
  storageId: "default"
databases:
- id: "default"
  host: "preinstall-plugin-test-bad-host"
  port: 5432
kafka: { }
workers: { }
