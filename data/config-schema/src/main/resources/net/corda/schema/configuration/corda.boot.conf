corda {
    # bootstrap parameters - ONLY for accessing the rest of the configuration
    # these will generally be provided via a command line or environment provided to the process
    boot {
    }
}
