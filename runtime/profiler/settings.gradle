pluginManagement {
    plugins {
        id 'com.jfrog.artifactory' version artifactoryPluginVersion
    }
}

rootProject.name = 'yourkit-profiler'

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
