postgresql:
  primary:
    nodeSelector: { node: "prereqs" }
    tolerations:
      - effect: "NoSchedule"
        key: "node"
        value: "prereqs"
nodeSelector: { node: "prereqs" }
tolerations:
  - effect: "NoSchedule"
    key: "node"
    value: "prereqs"